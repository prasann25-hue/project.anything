{
  "name": "careerpilot-ai-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "npx -y concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  }
}
